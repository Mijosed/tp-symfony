{% extends 'base.html.twig' %}

{% block title %}Modifier le Menu{% endblock %}

{% block content %}
<div class="container mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold text-red-600 mb-6">Modifier le Menu</h1>

    <div class="bg-white shadow-md rounded-lg p-6 border border-gray-200">
        {{ include('menu/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="mt-6 flex justify-between items-center">
        <a href="{{ path('app_menu_index') }}" class="text-blue-500 hover:underline">← Retour à la liste</a>
        {{ include('menu/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
