<nav class="bg-red-600 text-white shadow-md">
    <div class="container mx-auto px-4 py-4 flex justify-between items-center">
        <!-- Logo -->
        <a href="{{path('app_home')}}" class="text-2xl font-bold hover:text-gray-100">
            <span class="text-white">Restau</span><span class="text-yellow-300">TP</span>
        </a>

        <!-- Navigation Links -->
        <ul class="flex space-x-8 text-lg font-medium">
            <li><a href="{{path('app_home')}}" class="hover:text-yellow-300">Accueil</a></li>
            <li><a href="#" class="hover:text-yellow-300">Restaurants</a></li>
            <li><a href="#" class="hover:text-yellow-300">Tables</a></li>
            <li><a href="#" class="hover:text-yellow-300">Menus</a></li>
            <li><a href="#" class="hover:text-yellow-300">Réservations</a></li>
            <li><a href="{{ path('app_contact') }}" class="hover:text-yellow-300">Contact</a></li>

            <!-- Admin Link -->
            {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{path('admin_dashboard')}}" class="hover:text-yellow-300">Admin</a></li>
            {% endif %}
        </ul>

        <!-- Authentication Links -->
        <div class="flex items-center space-x-4">
            {% if app.user %}
                <!-- User Info -->
                <span>Bienvenue, <strong>{{ app.user.username }}</strong></span>
                <a href="{{ path('app_user_profile') }}" class="hover:text-yellow-300">Profil</a>
                <a href="{{ path('app_logout') }}" class="hover:text-yellow-300">Logout</a>
            {% else %}
                <!-- Login/Register Links -->
                <a href="{{ path('app_login') }}" class="hover:text-yellow-300">Login</a>
                <a href="{{ path('app_register') }}" class="hover:text-yellow-300">Register</a>
            {% endif %}
        </div>
    </div>
</nav>
