{% extends 'base.html.twig' %}

{% block title %}Email de Réinitialisation Envoyé{% endblock %}

{% block content %}
<div class="container mx-auto px-4 py-8">
    <div class="bg-white shadow-md rounded-lg p-6 border border-gray-200 text-center">
        <h1 class="text-2xl font-bold text-green-600 mb-4">Email envoyé avec succès</h1>
        <p class="text-gray-700 text-lg mb-4">
            Si un compte correspondant à votre email existe, un lien pour réinitialiser votre mot de passe vient de vous être envoyé.
        </p>
        <p class="text-gray-500 text-sm mb-6">
            Ce lien expirera dans 
            <span class="font-medium text-gray-700">
                {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </span>
        </p>
        <p class="text-gray-700 text-lg mb-4">
            Si vous ne recevez pas d'email, vérifiez votre dossier de spam ou 
            <a href="{{ path('app_forgot_password_request') }}" class="text-blue-500 hover:underline">
                réessayez
            </a>.
        </p>
    </div>
</div>
{% endblock %}
