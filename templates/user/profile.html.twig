{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}

{% block content %}
<div class="container mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold text-red-600 mb-6">Mon Profil</h1>

    <div class="bg-white shadow-md rounded-lg p-6 border border-gray-200">
        <p class="mb-4"><strong>Nom d'utilisateur :</strong> {{ user.username }}</p>
        <p class="mb-4"><strong>Email :</strong> {{ user.email }}</p>

        {% if user.roles is defined %}
            <p class="mb-4"><strong>Rôles :</strong> {{ user.roles|join(', ') }}</p>
        {% endif %}

    </div>
</div>
{% endblock %}
